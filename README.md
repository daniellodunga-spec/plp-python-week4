In Part A, I created the `grade_reporter.py` script. I set up a loop to process each score from a list and used conditional statements to assign letter grades. I also wrote logic to track the number of passes and fails, and summed the scores to calculate the average, rounded to one decimal place. For Part B, I worked on the `bug_hunt.py` script. I identified and fixed three bugs: I added the missing colon to the `while` statement, updated the condition to correctly include five, and converted the total variable to a string for printing.
The loop condition being set to less than five instead of less than or equal to five , was the bug that allowed the program to run without errors while producing the incorrect result. I knew there was a bug because it printed 10 as an answer instead of 15.
